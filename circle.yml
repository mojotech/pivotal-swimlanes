machine:
  node:
    version: 6.5

dependencies:
  pre:
    - npm install -g npm@3.10.3
  override:
    - nvm install 6.5 && npm run clean && npm install

test:
  override:
    - nvm use 6.5 && npm run lint && npm test
